// The spacing inside `{…}` might look a bit weird after sorting:
import {foo, bar, baz} from "example";

// The indentation (tabs) will be kept here:
import {
	DocumentNode,
	OperationDefinitionNode,
	SelectionSetNode,
	FieldNode,
	FragmentSpreadNode,
	InlineFragmentNode,
	FragmentDefinitionNode,
} from "../language/ast";
